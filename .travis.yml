sudo: false

addons:
  apt:
    packages:
      - bash-completion

install:
  - gem install bashcov
  - gem install coveralls -v 0.8.9

script: PATH="$PWD/bats/bin:$PATH" bashcov -- bats --tap test

notifications:
  email:
    on_success: never
