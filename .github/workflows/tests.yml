name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
      with:
        submodules: true
    - name: Run the tests
      env:
        REPO_TOKEN: ${{ secrets.CoverallsRepoToken }}
      run: |
        mkdir -p coverage
        docker pull kcov/kcov
        docker run --rm --security-opt seccomp=unconfined -v $(pwd):/source kcov/kcov \
          kcov --clean --bash-dont-parse-binary-dir --coveralls-id=$REPO_TOKEN /source/coverage --include-path=/source -- \
          /source/bats/bin/bats --tap /source/test
